# Access bootfs dir on Raspberry pi SD card
# edit cmdline.txt
  # add to end of file
    # cgroup_memory=1 cgroup_enable=memory
# edit config.txt
  # add to end of file
    # arm_64bit=1
# SSH into Raspberry Pi
# Become root user
  # sudo su -
# Run as root user
  # curl -sfl https://get.k3s.io | K3S_KUBECONFIG_MODE="644" sh -s

# Confirm it worked
  # kubectl get nodes

# master node is now set-up 

#During initial setup (headless)
#Add WiFi credentials to the SD card before first boot by creating wpa_supplicant.conf in the boot partition:

sudo nano /etc/wpa_supplicant/wpa_supplicant.conf




#Add these lines to wpa_supplicant.conf

country=US
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1

network={
    ssid="YourNetworkName"
    psk="YourPassword"
}
